<!--<manifest xmlns:android="http://schemas.android.com/apk/res/android">-->
<!--    &lt;!&ndash; The INTERNET permission is required for development. Specifically,-->
<!--         the Flutter tool needs it to communicate with the running application-->
<!--         to allow setting breakpoints, to provide hot reload, etc.-->
<!--    &ndash;&gt;-->
<!--    <uses-permission android:name="android.permission.INTERNET"/>-->
<!--</manifest>-->



<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Flutter hot reload and network requests -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Agora required permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Bluetooth (Android 12+ requires CONNECT only) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Older devices fallback (safe to keep, ignored on Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application>
        <!-- Your app config here -->
    </application>

</manifest>
